pull_request_rules:
  - name: VM4 trusted copy false-green auto-merge
    conditions:
      - author = mergify[bot]
      - base = main
      - head ~= ^mergify/copy/main/pr-
      - check-success = @github-actions/VM4 trusted copy false-green
    actions:
      merge:
        method: merge
